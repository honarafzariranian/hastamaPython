# Session signing key — generate with: python -c "import secrets; print(secrets.token_hex(48))"
SECRET_KEY=

# Debug mode — set to False in production
DEBUG=False

# HMAC secret for recovery codes — generate with: python -c "import secrets; print(secrets.token_hex(32))"
HASTAMA_HMAC_SECRET=

MODEL_PATH=./ml/model/
MODEL_NAME=model.pkl
MEMOIZATION_FLAG=True

# Araz device bridge secret — set a strong random value in production
ARAZ_ACCESS_PASSWORD=
